{
  "name": "playwright-mcp-framework",
  "version": "1.0.0",
  "description": "Playwright TypeScript framework with modular structure for scalable UI automation",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "pretest": "npm run clean",
    "clean": "rm -rf playwright-report test-results allure-results"
  },
  "keywords": [
    "playwright",
    "typescript",
    "testing",
    "automation",
    "e2e"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
